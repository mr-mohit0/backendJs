#!/usr/bin/env node
import { ppath, npath, constants, PortablePath, xfs, CwdFS, Filename } from '@yarnpkg/fslib';
import { ZipFS } from '@yarnpkg/libzip';
import { spawn } from 'child_process';
import { runExit, Command, Option, UsageError } from 'clipanion';
import * as t from 'typanion';
import { PassThrough } from 'stream';
import tar from 'tar';

async function bufferStream(stream) {
    return await new Promise((resolve, reject) => {
        const chunks = [];
        stream.on(`error`, error => {
            reject(error);
        });
        stream.on(`data`, chunk => {
            chunks.push(chunk);
        });
        stream.on(`end`, () => {
            resolve(Buffer.concat(chunks));
        });
    });
}
async function* parseTar(tgz) {
    const parser = new tar.Parse();
    const passthrough = new PassThrough({ objectMode: true, autoDestroy: true, emitClose: true });
    parser.on(`entry`, (entry) => {
        passthrough.write(entry);
    });
    parser.on(`error`, error => {
        passthrough.destroy(error);
    });
    parser.on(`close`, () => {
        if (!passthrough.destroyed) {
            passthrough.end();
        }
    });
    parser.end(tgz);
    for await (const entry of passthrough) {
        const it = entry;
        yield it;
        it.resume();
    }
}
async function extractArchiveTo(tgz, targetFs, { stripComponents = 0, prefixPath = PortablePath.dot } = {}) {
    var _a, _b;
    function ignore(entry) {
        // Disallow absolute paths; might be malicious (ex: /etc/passwd)
        if (entry.path[0] === `/`)
            return true;
        const parts = entry.path.split(/\//g);
        // We also ignore paths that could lead to escaping outside the archive
        if (parts.some((part) => part === `..`))
            return true;
        if (parts.length <= stripComponents)
            return true;
        return false;
    }
    for await (const entry of parseTar(tgz)) {
        if (ignore(entry))
            continue;
        const parts = ppath.normalize(npath.toPortablePath(entry.path)).replace(/\/$/, ``).split(/\//g);
        if (parts.length <= stripComponents)
            continue;
        const slicePath = parts.slice(stripComponents).join(`/`);
        const mappedPath = ppath.join(prefixPath, slicePath);
        let mode = 0o644;
        // If a single executable bit is set, normalize so that all are
        if (entry.type === `Directory` || (((_a = entry.mode) !== null && _a !== void 0 ? _a : 0) & 0o111) !== 0)
            mode |= 0o111;
        switch (entry.type) {
            case `Directory`:
                {
                    targetFs.mkdirpSync(ppath.dirname(mappedPath), { chmod: 0o755, utimes: [constants.SAFE_TIME, constants.SAFE_TIME] });
                    targetFs.mkdirSync(mappedPath, { mode });
                    targetFs.utimesSync(mappedPath, constants.SAFE_TIME, constants.SAFE_TIME);
                }
                break;
            case `OldFile`:
            case `File`:
                {
                    targetFs.mkdirpSync(ppath.dirname(mappedPath), { chmod: 0o755, utimes: [constants.SAFE_TIME, constants.SAFE_TIME] });
                    targetFs.writeFileSync(mappedPath, await bufferStream(entry), { mode });
                    targetFs.utimesSync(mappedPath, constants.SAFE_TIME, constants.SAFE_TIME);
                }
                break;
            case `SymbolicLink`:
                {
                    targetFs.mkdirpSync(ppath.dirname(mappedPath), { chmod: 0o755, utimes: [constants.SAFE_TIME, constants.SAFE_TIME] });
                    targetFs.symlinkSync(entry.linkpath, mappedPath);
                    (_b = targetFs.lutimesSync) === null || _b === void 0 ? void 0 : _b.call(targetFs, mappedPath, constants.SAFE_TIME, constants.SAFE_TIME);
                }
                break;
        }
    }
    return targetFs;
}

const isModuleLocator = t.cascade(t.isString(), [t.matchesRegExp(/^[^:]+(:[^:]+)?$/)]);
async function isDirectory(path) {
    try {
        const stat = await xfs.statPromise(path);
        return stat.isDirectory();
    }
    catch (err) {
        if (err.code === `ENOENT`) {
            return false;
        }
        else {
            throw err;
        }
    }
}
class PackCommand extends Command {
    constructor() {
        super(...arguments);
        this.input = Option.String(`-i,--input`, {
            description: `Path of the .tgz archive which will be turned into a SEA executable`,
        });
        this.install = Option.String(`-c,--install-command`, {
            description: `If set, the specified command will run on the given archive before generating the SEA archive`,
        });
        this.output = Option.String(`-o,--output`, {
            description: `The location where to store the resulting SEA executable`,
        });
        this.binary = Option.String(`-b,--bin`, {
            description: `Name of the binary that must be called when executing the SEA archive`,
            validator: isModuleLocator,
        });
        this.main = Option.String(`-m,--main`, {
            description: `Name of the module that must be forwarded when requiring the SEA archive`,
            validator: isModuleLocator,
        });
    }
    async execute() {
        var _a;
        const input = typeof this.input !== `undefined`
            ? await xfs.readFilePromise(npath.toPortablePath(this.input))
            : undefined;
        const zipFs = await this.generateZipArchive(input);
        const seaFile = await this.generateSeaFile(zipFs);
        let outputPath = npath.toPortablePath((_a = this.output) !== null && _a !== void 0 ? _a : `./`);
        if (outputPath.endsWith(`/`) || await isDirectory(outputPath))
            outputPath = ppath.join(outputPath, `a`);
        if (!ppath.extname(outputPath))
            outputPath = `${outputPath}.cjs`;
        await xfs.writeFilePromise(outputPath, seaFile);
    }
    async generateZipArchive(input) {
        const zipFs = new ZipFS();
        const prefixPath = `package`;
        const install = this.install;
        if (typeof install === `undefined`) {
            if (typeof input !== `undefined`)
                await extractArchiveTo(input, zipFs, { stripComponents: 1, prefixPath });
            return zipFs;
        }
        await xfs.mktempPromise(async (tempDir) => {
            const pkgFolder = ppath.join(tempDir, prefixPath);
            await xfs.mkdirPromise(pkgFolder);
            const targetFs = new CwdFS(pkgFolder);
            // 1. Extract the tgz into a temporary folder
            if (typeof input !== `undefined`)
                await extractArchiveTo(input, targetFs, { stripComponents: 1 });
            // 2. Remove the development dependencies and scripts; we only care about installing dependencies
            const data = await this.openManifest(targetFs);
            delete data.devDependencies;
            delete data.scripts;
            await targetFs.writeJsonPromise(Filename.manifest, data);
            // 3. Run the install command there
            const child = spawn(install, [], {
                cwd: npath.fromPortablePath(pkgFolder),
                shell: true,
                stdio: `inherit`,
                env: {
                    ...process.env,
                    // This node-sea prototype only deals with node_modules for now, since
                    // that's the common denominator between all three package managers
                    YARN_NODE_LINKER: `node-modules`,
                },
            });
            const result = await new Promise((resolve, reject) => {
                child.on(`close`, (code, signal) => resolve(code !== null && code !== void 0 ? code : 1));
            });
            if (result !== 0)
                throw new UsageError(`Command failed`);
            // 4. Pack the resulting folder into the zip archive
            await zipFs.copyPromise(PortablePath.root, tempDir, { baseFs: xfs });
        });
        return zipFs;
    }
    async generateSeaFile(zipFs) {
        var _a, _b;
        const zipData = zipFs.getBufferAndClose();
        const templatePath = npath.toPortablePath(require.resolve(`../template`));
        const templateContent = await xfs.readFilePromise(templatePath, `utf8`);
        const templatePrefix = `"use strict";var parameters=${JSON.stringify({
            data: zipData.toString(`base64`),
            main: (_a = this.main) === null || _a === void 0 ? void 0 : _a.split(`:`),
            bin: (_b = this.binary) === null || _b === void 0 ? void 0 : _b.split(`:`),
        })};\n`;
        return templatePrefix + templateContent;
    }
    async openManifest(targetFs, p = PortablePath.dot) {
        let data;
        try {
            data = await targetFs.readJsonPromise(ppath.join(p, Filename.manifest));
        }
        catch { }
        return data !== null && data !== void 0 ? data : {};
    }
}
runExit({
    binaryLabel: ``,
}, [
    PackCommand,
]);
